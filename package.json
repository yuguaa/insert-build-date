{
  "name": "insert-build-date",
  "version": "1.0.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "pnpm --filter insert-build-date build",
    "release": "bumpp package.json packages/core/package.json && pnpm --filter insert-build-date publish --access public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "bumpp": "^8.2.1",
    "tsup": "^6.6.0"
  }
}
